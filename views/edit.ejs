<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body> 
    <div class="w-full h-screen  bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-600 rounded-md p-2" href="/logout">logout</a>
        </div>
        <h5 class="mb-3">Update your post</h5>
        <form action="/update/<%= post._id%>" method="post">
            <textarea class="resize-none bg-transparent outline-none rounded-md border-2 border-zinc-600 w-1/3 p-2 block" placeholder="whats on your mind" name="content"><%= post.content %></textarea>
            <input class="bg-yellow-500 rounded-md w-40 p-2 text-sm block mt-2" type="submit" value="Update post">
        </form>

        
    </div>
</body>
</html>